<!DOCTYPE html>
<html>
<head>
  <title>Rubocop results</title>
  <style type="text/css" media="screen">
  
    body {
      font: normal 1em Verdana, sans-serif;
      margin: 0;
      body: 0;
    }
    
    h1, h2, h3, th {
      font-weight: 600;
      font-size: 1em;
    }
    
    ol, ol li {
      list-style: none;
      margin: 0;
      padding: 0;
    }
    
    a {
      display: inline-block;
      padding: 0.25em;
    }
    
    /*    
      Offences, posible values...
      * refactor 
      * convention 
      * warning 
      * error
      * fatal
    */
    
    .status {
      padding: 0.5em 1em;
    }
        
    .refactor {
      background-color: #FB4F5F;
    }
    
    .convention {
    }
    
    .warning {
      background-color: #FFFA65;
    }
    
    .error {
      background-color: #FF6700;
    }
    
    .fatal {
      color: #fff;
      background-color: #CC0920;
    }
    
    .good {
      background-color: #73B532;
    }
    
    .file_report {
      margin-bottom: 1em;
    }
    
    .cop_name, #metadata {
      color: #BFBFBF;
      font-size: 0.75em;
    }

    #metadata {
      margin-top: 1em;
    }
    
    #metadata th, #metadata td {
      text-align: left;
      padding-right: 0.75em;
    }
    
  </style>
</head>
<body>
  
  <h1><%= offences_message %> from <%= file_count %></h1>
  
  <% @files.each do |f| %>
    <div class="file_report">
      <h4><a href="<%= url_to(f) %>"><%= filename(f) %></a></h4>
      <% if @summary[:offence_count].zero? %>
        <%= partial '_all_good.erb' %>
      <% else %>
        <%= partial '_offences.erb', {:f => f} %>
      <% end %>
    </div>
  <% end %>

  <table id="metadata">
    <% metadata.each_pair do |key, value| %>
      <tr><th><%= key %></th><td><%= value %></td></tr>
    <% end %>
  </table>

</body>
</html>